[project]
name = "sentricoon"
version = "0.0.1"
description = "Safety-first OS technician AI agent for Linux and Windows, starting with Ubuntu repair"
readme = "README.md"
license = "MIT"
authors = [{name = "BlaKity"}]
requires-python = ">=3.10"
dependencies = []

[dependency-groups]
dev = []

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-q"
